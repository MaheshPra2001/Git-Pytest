name: Print Message

on:
  push:
      branches:
        - 'main'

jobs:
    print-message:
        runs-on: ubuntu-latest

        steps:
            - name: print Message
              run: echo "new Push detected on mian branch"