name: Print Message

on:
  push:
      branches:
        - 'master'

jobs:
    print-message:
        runs-on: ubuntu-latest

        steps:
            - name: print Message
              run: echo "new Push detected on mian branch"